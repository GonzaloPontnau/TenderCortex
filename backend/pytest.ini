[pytest]
# Pytest configuration for TenderCortex

# Test paths
testpaths = tests

# Python files pattern
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Asyncio mode for pytest-asyncio
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Output settings
addopts = -v --tb=short

# Markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    bdd: marks tests as BDD behavior specs
    spec: marks tests as specification-driven tests
